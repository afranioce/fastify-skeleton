version: '3.7'
services:
  app:
    image: 'fastify-skeleton:latest'
    build:
      context: .
      dockerfile: docker/node/Dockerfile
      target: development
      args:
        - NPM_TOKEN=${NPM_TOKEN}
    entrypoint: "yarn run watch"
    ports:
      - 4444:4444
    volumes:
      - .:/app
